import React from "react"
import { ScrollShadow, ScrollShadowProps } from "@heroui/react"

export const KaniScrollShadow = (props: ScrollShadowProps) => {
    return (
        <ScrollShadow {...props}/>
    )
}