import { Select, SelectItem, SelectProps } from "@heroui/react"
import React from "react"

export const KaniSelect = (props: SelectProps) => {
    return <Select {...props} />
}
export const KaniSelectItem = SelectItem